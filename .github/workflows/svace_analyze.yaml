name: Svace analyze

on:
  schedule:
    - cron: "0 12 * * 6" # Regular static analysis
  workflow_dispatch:

jobs:
  build_dev:
    uses: ./.github/workflows/build_dev.yml
    secrets: inherit
    with:
      svace_enabled: true
