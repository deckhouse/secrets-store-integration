name: secrets-store-integration
namespace: d8-secrets-store-integration
stage: General Availability
weight: 930
descriptions:
  en: |
    Secrets store integration module is designed
    to add secrets from a Vault-compatible store to containers
  ru: |
    Модуль secrets-store-integration реализует интеграцию хранилища
    секретов и приложений в Kubernetes-кластерах
requirements:
  bootstrapped: true
  deckhouse:  ">= 1.71"
